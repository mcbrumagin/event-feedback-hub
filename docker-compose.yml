version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: packages/api/Dockerfile
    container_name: event-feedback-hub-api
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=production
      - PORT=4000
      - HOST=0.0.0.0
      - JWT_SECRET=${JWT_SECRET:-change-this-in-production}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-admin123}
      - CORS_ORIGIN=http://localhost:3000
      - LOG_LEVEL=info
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:4000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
        labels: "service"
    labels:
      - "service=event-feedback-hub-api"

  frontend:
    build:
      context: .
      dockerfile: packages/frontend/Dockerfile
    container_name: event-feedback-hub-frontend
    ports:
      - "3000:80"
    depends_on:
      api:
        condition: service_healthy
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
        labels: "service"
    labels:
      - "service=event-feedback-hub-frontend"

# Future: Add MongoDB service
#   mongodb:
#     image: mongo:7
#     container_name: event-feedback-hub-mongodb
#     ports:
#       - "27017:27017"
#     environment:
#       - MONGO_INITDB_ROOT_USERNAME=admin
#       - MONGO_INITDB_ROOT_PASSWORD=password
#     volumes:
#       - mongodb_data:/data/db
#     # Note: For change streams, MongoDB needs to run as replica set
#     command: ["--replSet", "rs0"]
#
# volumes:
#   mongodb_data:
